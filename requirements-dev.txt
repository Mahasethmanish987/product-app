# Development + CI dependencies. NOT installed into the production image.
-r requirements.txt

pytest==9.1.1
httpx==0.28.1          # required by fastapi.testclient
ruff==0.16.6           # linter + formatter
